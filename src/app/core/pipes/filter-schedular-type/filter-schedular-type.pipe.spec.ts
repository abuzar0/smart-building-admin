import { FilterSchedularTypePipe } from './filter-schedular-type.pipe';

describe('FilterSchedularTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSchedularTypePipe();
    expect(pipe).toBeTruthy();
  });
});
