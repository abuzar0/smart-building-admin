import { FilterDeviceTypePipe } from './filter-device-type.pipe';

describe('FilterDeviceTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDeviceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
