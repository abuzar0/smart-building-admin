import { FilterUserTypePipe } from './filter-user-type.pipe';

describe('FilterUserTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUserTypePipe();
    expect(pipe).toBeTruthy();
  });
});
