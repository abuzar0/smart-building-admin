import { NumbertoSymblePipe } from './numberto-symble.pipe';

describe('NumbertoSymblePipe', () => {
  it('create an instance', () => {
    const pipe = new NumbertoSymblePipe();
    expect(pipe).toBeTruthy();
  });
});
