import { FilterRoomTypePipe } from './filter-room-type.pipe';

describe('FilterRoomTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRoomTypePipe();
    expect(pipe).toBeTruthy();
  });
});
