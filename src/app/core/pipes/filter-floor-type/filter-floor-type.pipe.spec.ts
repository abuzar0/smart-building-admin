import { FilterFloorTypePipe } from './filter-floor-type.pipe';

describe('FilterFloorTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFloorTypePipe();
    expect(pipe).toBeTruthy();
  });
});
