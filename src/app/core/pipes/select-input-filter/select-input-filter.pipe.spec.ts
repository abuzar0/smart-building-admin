import { SelectInputFilterPipe } from './select-input-filter.pipe';

describe('SelectInputFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectInputFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
